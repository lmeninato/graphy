Simple graphing in sigma.js and flask.

To run (make sure you have flask installed):

```
git clone https://github.com/lmeninato/graphy.git
cd graphy
export FLASK_APP=graphy
export FLASK_ENV=development

flask run
```
